language: python
cache:
  pip: true
services:
    - docker
python:
    - "3.6"
env:
    - TOXENV=py36
script:
- tox
cache:
    directories:
        - $HOME/.cache/pre-commit
        - $HOME/.cache/pip
        - $HOME/.git/hooks/pre-commit
