"""
A config file to be imported into python files
for passwords and usernames if needed.
The example document is merely an example,
locally one should have a config.py with the passwords needed with
config_example.py as a template.

With OAuh it is generally not necessary,
but in the case that we need to private
message then it may be.
"""

passwords = {
    'reddit_password': 'xxx',
    'reddit_secret': 'xxx',
}

test_user = {
    'reddit_test_user': 'xxx',
}
